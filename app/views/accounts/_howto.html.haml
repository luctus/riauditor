%h2 How to get AWS AIM access key?

%pre.code
  :escaped
                {
      "Statement": [
        {
          "Action": [
            "ec2:DescribeInstances",
            "ec2:DescribeReservedInstances",
          ],
          "Effect": "Allow",
          "Resource": "*"
        }
      ]
    }
